# Docs

Public documentation for Knowlith currently lives in the web app routes under `apps/web` (see `/docs` and `/docs/language-of-trust`).

This folder is reserved for future standalone docs sources (Markdown site, exported references). Until then, edit copy in the React docs pages.
